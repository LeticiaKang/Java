package kr.brain07;

public class Trapezoid extends Polygon{

	public Trapezoid(int height,int width, int top) {
		super(height, width, top);
	}
	
	public int evaluate() {
		int area = ( getTop() + getWidth() ) *  getHeight() / 2;
		return area;
	}
}
